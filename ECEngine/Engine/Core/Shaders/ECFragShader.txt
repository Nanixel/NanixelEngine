#version 420 core

out vec4 FragColor;

in vec2 textureCoord;

uniform vec4 color;
uniform vec4 lightColor;

uniform sampler2D sprite;

void main()
{
	//FragColor = color * lightColor;
    FragColor = color * lightColor * texture(sprite, textureCoord);
}